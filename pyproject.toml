[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ha-tools"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "websockets>=13.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ha_tools"]
